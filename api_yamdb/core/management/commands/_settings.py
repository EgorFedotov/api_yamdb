from reviews.models import Category, Genre, Title, GenreTitle

#TODO:
model_by_filename = {
    #'comments': Comment,
    'category': Category,
    'genre': Genre,
    'titles': Title,
    'genre_title': GenreTitle,
    #'review': Review,
    #'users': User,
}
