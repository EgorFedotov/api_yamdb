from reviews.models import Category, Genre, Title

#TODO:
model_by_filename = {
    #'comments': Comment,
    'category': Category,
    'genre': Genre,
    #'genre_title': GenreTitle,
    #'review': Review,
    'titles': Title,
    #'users': User,
}
