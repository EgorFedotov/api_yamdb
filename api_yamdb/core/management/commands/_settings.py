from api.models import Category

model_by_filename = {
    'comments': Comment,
    'category': Category,
    'genre': Genre,
    'genre_title': GenreTitle,
    'review': Review,
    'titles': Title,
    'users': User,
}
